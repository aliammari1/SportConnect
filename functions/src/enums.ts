export enum NotificationType {
    Message = "message",
    RideRequest = "ride_request",
    RideUpdate = "ride_update",
}

export enum NotificationChannelId {
    Messages = "sport_connect_messages",
    Rides = "sport_connect_rides",
    General = "sport_connect_general",
}